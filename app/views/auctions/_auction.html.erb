<tr>
  <td><%= link_to auction.description, auction %></td>    
  <td><%= auction.url %></td>
  <td><%= auction.max_bids %></td>
  <td><%= link_to 'Edit', edit_auction_path(auction) %> | 
      <%= link_to 'Delete', auction, method: :delete, data: { confirm: 'Are you sure?' } %>
  </td>
</tr>